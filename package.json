{
  "name": "fantasy-dynasty-analyzer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests/python -p 'test_*.py'",
    "serve": "python3 -m http.server 4173 --directory docs"
  }
}
